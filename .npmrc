# Reproducible, safe installs.
save-exact=false
engine-strict=true
# Do not run arbitrary lifecycle scripts from transitive deps by default.
# (pnpm v10 requires explicit allow-list via package.json "pnpm.onlyBuiltDependencies".)
