[package]
name = "transport-core"
description = "Transport adapter traits, bounded buffers, and passive-capture contract."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]

