{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 1 minimal capability set: core window only. No filesystem, shell, or network plugins enabled.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
