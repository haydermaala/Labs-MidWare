# docs-site (placeholder)

Static documentation site. Tooling selection is deferred (an ADR will record the
choice) until there is enough published content to warrant it. For now, authored
docs live in `docs/`.
